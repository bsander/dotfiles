- link:
    ~/df/.zshrc:
      create: true
      relink: true

- clean: ['~', '~/df']

- shell:
    # - [DOTFILES=. ZDOTDIR=~/df zsh -c "zplug update", Updating zplug]
    - [git submodule update --init --recursive, Installing submodules]

# - brewfile:
#     file: Brewfile
