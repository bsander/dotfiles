- defaults:
    shell:
      stdout: true
      stderr: true

- shell:
    - description: Self-updating dotbot
      command: git submodule update --remote
    - description: Self-updating fish plugin manager
      command: fisher self-update
    - description: Updating fish shell plugins
      command: fisher

